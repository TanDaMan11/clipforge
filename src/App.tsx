import { useState } from 'react';
import { Sparkles, Download, Search, Loader2 } from 'lucide-react';
import JSZip from 'jszip';

type C = { title: string; hook: string; viralityScore: number; summary: string; startTime: number; endTime: number };
const demo = (source: string): C[] => Array.from({ length: 5 }, (_, i) => ({
  title: `${source.slice(0, 34) || 'The video'} — moment ${i + 1}`,
  hook: ['You need to see what happens next.', 'This changes everything.', 'Nobody talks about this part.', 'The most surprising moment.', 'Here is the real takeaway.'][i],
  viralityScore: 90 - i * 3,
  summary: `Dynamic clip ${i + 1} selected from the video context.`,
  startTime: i * 60,
  endTime: i * 60 + 42,
}));
const ID = '[a-zA-Z0-9_-]{11}';
const BROAD = new RegExp(`(?:v=|/v/|youtu\\.be/|/shorts/|/embed/|/live/|%3Dv%3D|%3D)(${ID})`, 'i');
const DIRECT = new RegExp(`^${ID}$`);
function getYouTubeId(value: string) { const d = decodeURIComponent(value.trim()); const m = d.match(BROAD)?.[1]; if (m) return m; if (DIRECT.test(d)) return d; try { const p = new URL(d.includes('://') ? d : `https://${d}`); return p.searchParams.get('v')?.match(new RegExp(`^${ID}`))?.[0] || p.pathname.match(new RegExp(`^/(?:shorts|embed|live)/(${ID})`))?.[1] || ''; } catch { return ''; } }
const stamp = (n: number) => { const s = Math.max(0, Math.round(Number(n) || 0)); return `${String(Math.floor(s / 3600)).padStart(2, '0')}:${String(Math.floor(s / 60) % 60).padStart(2, '0')}:${String(s % 60).padStart(2, '0')}`; };
function parseClips(raw: string): C[] | null { try { const text = raw.match(/\\[[\\s\\S]*\\]/)?.[0] || raw; const a = JSON.parse(text); if (!Array.isArray(a) || a.length < 5) return null; const out = a.slice(0, 5).map((x: any, i) => ({ title: String(x.title || `Clip ${i + 1}`), hook: String(x.hook || 'Watch this.'), viralityScore: Math.max(0, Math.min(100, Number(x.viralityScore ?? x.virality_score) || 70)), summary: String(x.summary || x.segment_text || x.reasoning || ''), startTime: Math.max(0, Number(x.startTime ?? x.start_time) || i * 60), endTime: Number(x.endTime ?? x.end_time) || i * 60 + 30 })); return out.every(x => x.endTime > x.startTime) ? out : null; } catch { return null; } }
async function pollinate(source: string): Promise<C[]> {
  const prompt = `Return ONLY valid JSON, an array of exactly 5 video-specific clips. Every object must have title, hook, startTime (number seconds), endTime (number seconds greater than startTime), viralityScore (0-100), and summary. Use the supplied transcript or metadata; never use markdown. Source: ${source}`;
  const urls = [`https://text.pollinations.ai/${encodeURIComponent(prompt)}`, `https://text.pollinations.ai/`];
  for (const url of urls) try { const r = await fetch(url, url.endsWith('/') ? { method: 'POST', headers: { 'Content-Type': 'application/json', Accept: 'application/json' }, body: JSON.stringify({ messages: [{ role: 'user', content: prompt }], model: 'openai', jsonMode: true }) } : undefined); if (r.ok) { const clips = parseClips(await r.text()); if (clips) return clips; } } catch { /* use deterministic fallback */ }
  return demo(source);
}
async function metadata(id: string) { try { const r = await fetch(`https://www.youtube.com/oembed?url=${encodeURIComponent(`https://www.youtube.com/watch?v=${id}`)}&format=json`); if (r.ok) return await r.json(); } catch {} return { title: `YouTube video ${id}`, author_name: 'YouTube creator' }; }
export default function App() { const [u, setU] = useState(''), [clips, setClips] = useState<C[]>([]), [selected, setSelected] = useState<C | null>(null), [busy, setBusy] = useState(false), [msg, setMsg] = useState(''); async function load() { const id = getYouTubeId(u); if (!id) return setMsg('Invalid YouTube URL.'); setBusy(true); setMsg('Generating 5 dynamic clips…'); const m = await metadata(id); const result = await pollinate(`Video title: ${m.title}. Author: ${m.author_name}. Video ID: ${id}.`); setClips(result); setSelected(result[0]); setMsg('Generated 5 clips with valid timestamps and captions.'); setBusy(false); } async function download() { const z = new JSZip(); clips.forEach((c, i) => z.file(`clip-${i + 1}.srt`, `1\\n${stamp(c.startTime)},000 --> ${stamp(c.endTime)},000\\n${c.summary}`)); const b = await z.generateAsync({ type: 'blob' }); const a = document.createElement('a'); a.href = URL.createObjectURL(b); a.download = 'clipforge-captions.zip'; a.click(); } return <main className="min-h-screen grid-bg"><header className="max-w-6xl mx-auto p-6 flex justify-between"><b className="text-2xl">CLIP<span className="text-emerald-400">FORGE</span></b><button onClick={download}><Download /></button></header><div className="max-w-6xl mx-auto p-6"><div className="glass rounded-3xl p-8"><p className="text-emerald-400 font-mono">// AI CREATOR STUDIO</p><h1 className="text-5xl font-bold mt-3">Long-form in. <span className="text-emerald-400">Viral shorts out.</span></h1><div className="flex gap-3 mt-8"><div className="flex-1 flex gap-2 bg-zinc-950 border border-zinc-700 rounded-xl px-4"><Search /><input value={u} onChange={e => setU(e.target.value)} placeholder="YouTube URL" className="bg-transparent outline-none w-full py-4" /></div><button onClick={load} disabled={busy} className="bg-white text-black rounded-xl px-5">{busy ? <Loader2 className="animate-spin" /> : 'Load'}</button></div><p className="text-amber-300 mt-3">{msg}</p></div>{clips.length > 0 && <section className="mt-6"><h2 className="font-bold mb-3">Viral moments <span className="text-emerald-400">05</span></h2>{clips.map((c, i) => <button key={i} onClick={() => setSelected(c)} className="glass rounded-xl p-4 mb-3 w-full text-left"><small className="text-emerald-400">0{i + 1} · {stamp(c.startTime)}–{stamp(c.endTime)} · {c.viralityScore}% VIRAL</small><h3 className="font-bold mt-2">{c.title}</h3><p className="text-zinc-400">{c.hook}</p></button>)}{selected && <div className="glass rounded-2xl p-5"><p className="text-emerald-400">{selected.startTime}s – {selected.endTime}s</p><h2 className="text-2xl font-bold">{selected.title}</h2><p className="text-zinc-400 mt-2">{selected.summary}</p></div>}</section>}</div></main>; }
